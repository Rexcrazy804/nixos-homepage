---
interface Props {
    type?: "h1" | "h2" | "h3" | "h4",
    color?: "blue" | "white"
}

const { type = "h1" } = Astro.props;

const fontClasses = "font-bold font-heading leading-none"
---

{type == "h1" && <h1 class:list={["text-5xl", fontClasses]}><slot/></h1>}